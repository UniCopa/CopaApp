<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/subscription_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="List of all SingleEvents" />

    <Button
        android:id="@+id/singleeventlist_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/main_text"
        android:layout_marginTop="46dp"
        android:onClick="onSingleEventListButtonClick"
        android:text="@string/details_to_singleevent" />

    <Button
        android:id="@+id/allSingleEventsButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/singleeventlist_button"
        android:layout_alignBottom="@+id/singleeventlist_button"
        android:layout_toRightOf="@+id/subscription_text"
        android:onClick="onChangeDateButtonClick"
        android:text="@string/button_singleevent_change" />

    <ListView
        android:id="@+id/singleEventListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/singleeventlist_button"
        android:layout_below="@+id/singleeventlist_button" >

    </ListView>

</RelativeLayout>